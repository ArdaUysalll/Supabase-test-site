## Getting Started

First, run the development server:
```
install npm
Add the correct variables to the .env file (NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY)
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.


